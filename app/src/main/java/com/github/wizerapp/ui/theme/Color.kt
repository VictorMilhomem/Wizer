package com.github.wizerapp.ui.theme

import androidx.compose.ui.graphics.Color

// Cores primárias
val WizerBlue = Color(0xFF2196F3)         // Azul primário
val WizerLightBlue = Color(0xFF64B5F6)    // Azul claro
val WizerDarkBlue = Color(0xFF0D47A1)     // Azul escuro

// Cores de destaque
val WizerOrange = Color(0xFFFF9800)       // Laranja para ações e botões importantes
val WizerGreen = Color(0xFF4CAF50)        // Verde para confirmações e sucesso
val WizerRed = Color(0xFFE53935)          // Vermelho para erros e alertas

// Cores neutras
val WizerGray = Color(0xFF9E9E9E)         // Cinza para textos secundários
val WizerLightGray = Color(0xFFF5F5F5)    // Cinza claro para fundos
val WizerDarkGray = Color(0xFF424242)     // Cinza escuro para textos principais

// Cores de fundo para o tema claro e escuro
val WizerBackgroundLight = Color(0xFFFAFAFA)
val WizerBackgroundDark = Color(0xFF121212)

// Cores de superfície para o tema claro e escuro
val WizerSurfaceLight = Color(0xFFFFFFFF)
val WizerSurfaceDark = Color(0xFF1E1E1E)